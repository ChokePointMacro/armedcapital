# Deploy

Deploy the ArmedCapital platform.

## Steps
1. Run tests: `npx vitest --run`
2. Run TypeScript check: `npx tsc --noEmit`
3. Deploy frontend via Vercel
4. Deploy tradingbot via DigitalOcean (NYC)
